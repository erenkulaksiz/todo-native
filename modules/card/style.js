import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
    container: {
        width: "100%",
        backgroundColor: "#fff",
        borderRadius: 8,
        marginBottom: 12,
        padding: 12,
        borderStyle: "solid",
        borderWidth: 2,
        borderColor: "#dbdbdb",
    },
    cardTitle: {
        fontSize: 21,
    },
    cardDesc: {
        fontSize: 16,
        marginTop: 8,
    }
});