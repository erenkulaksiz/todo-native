import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
    container: {
        width: "100%",
        backgroundColor: "#fff",
        elevation: 4,
        borderRadius: 4,
        marginBottom: 12,
        padding: 12,
    },
    cardTitle: {
        fontSize: 21,
    },
    cardDesc: {
        fontSize: 16,
        marginTop: 8,
    }
});